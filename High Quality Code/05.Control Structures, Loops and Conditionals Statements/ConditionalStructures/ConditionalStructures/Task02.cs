//First if statement

Potato potato;

//... 

if (potato != null)
{
    if(potato.IsPeeled && !potato.IsRotten)
    {
        Cook(potato);
    } 
}

//Second if statement

bool inRangeY = (MIN_Y <= y && y <= MAX_Y);

if (x >= MIN_X) 
{
    if(x <= MAX_X && (inRangeY && canVisitCell)))
    {
        VisitCell();
    }
}